%initialization 

v=zeros(Nx,Ny);
rho=ones(Nx,Ny);
ke=zeros(Nx,Ny);
diss=zeros(Nx,Ny);
u1=u;
v1=v;
rho1=rho;
ke1=ke;
diss1=diss;
p=zeros(Nx,Ny);
nuT=zeros(Nx,Ny);
nu1=nuT;
nu0=nuT;
src=zeros(Nx,Ny);
y095=zeros(1,Nx);
y01=zeros(1,Nx);
y05=zeros(1,Nx);
delta=zeros(1,Nx);
yn=zeros(Nx,Ny);
dudy=zeros(Nx,Ny);
lmix=zeros(1,Nx);
